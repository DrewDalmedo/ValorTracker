<% if session[:user_id]%>
    <%= link_to 'New Guide', new_user_guide_path(User.find(session[:user_id])) %><br>
    <br>
<% end%>

<% @guides.each do |guide| %>
    <%= show_guide_link guide %><br>
<% end %>